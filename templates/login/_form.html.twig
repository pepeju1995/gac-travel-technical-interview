{{ form_start(loginForm) }}
    <div class="row">
        <div class="col-lg-12">
            <div class="floating-label form-group">
                {{ form_widget(loginForm.username, {'attr': {'class': 'floating-input form-control', 'placeholder': ''}}) }}
                {{ form_label(loginForm.username, 'Usuario') }}
            </div>
            <div class="floating-label form-group">
                {{ form_widget(loginForm.password, {'attr': {'class': 'floating-input form-control', 'placeholder': ''}}) }}
                {{ form_label(loginForm.password, 'Contraseña') }}
            </div>
        </div>
    </div>

    <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">
    <button type="submit" class="btn btn-primary">Acceder</button>
    <p class="mt-3">
        Crear una cuenta <a href="{{ path('app_register') }}" class="text-primary">Registrarse</a>
    </p>
{{ form_end(loginForm) }}